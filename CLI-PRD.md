# ai-toolkit PRD

## 1. 개요

**목적**: AI 코딩 에이전트들에게 Skills, Rules, Commands를 일괄 배포하는 CLI 도구

**핵심 명령어**:
```bash
npx ai-toolkit              # 인터랙티브 모드
npx ai-toolkit --skills     # Skills 직접 추가
npx ai-toolkit --rules      # Rules 직접 추가
npx ai-toolkit --commands   # Commands 직접 추가
```

---

## 2. 사용자 플로우

```
┌─────────────────────────────────────────────────────────────┐
│ 1. Type 선택 (또는 --flag로 직접 지정)                      │
│    ○ Skills   - SKILL.md 기반                              │
│    ○ Rules    - RULES.md 기반                              │
│    ○ Commands - COMMANDS.md 기반                           │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. Source 입력                                              │
│    - GitHub: owner/repo, https://github.com/owner/repo     │
│    - Local: ./path, /absolute/path                         │
│    - Direct URL: https://docs.example.com/skill.md         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. Resource 선택 (multi-select)                             │
│    ☑ skill-a - "Description of skill A"                    │
│    ☑ skill-b - "Description of skill B"                    │
│    ☐ skill-c - "Description of skill C"                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. Agent 선택 (multi-select)                                │
│    ☑ Claude Code                                           │
│    ☑ Cursor                                                │
│    ☐ Cline                                                 │
│    ... (23개 에이전트)                                      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. Scope 선택                                               │
│    ○ Project - 현재 디렉토리 (.claude/skills/)             │
│    ○ Global  - 홈 디렉토리 (~/.claude/skills/)             │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. 설치 실행 + 중복 처리                                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 지원 에이전트 (23개)

| Agent | Key | Project Path | Global Path |
|-------|-----|--------------|-------------|
| Amp | `amp` | `.agents/skills/` | `~/.config/agents/skills/` |
| Antigravity | `antigravity` | `.agent/skills/` | `~/.gemini/antigravity/skills/` |
| Claude Code | `claude-code` | `.claude/skills/` | `~/.claude/skills/` |
| Clawdbot | `clawdbot` | `skills/` | `~/.clawdbot/skills/` |
| Cline | `cline` | `.cline/skills/` | `~/.cline/skills/` |
| Codex | `codex` | `.codex/skills/` | `~/.codex/skills/` |
| Command Code | `command-code` | `.commandcode/skills/` | `~/.commandcode/skills/` |
| Cursor | `cursor` | `.cursor/skills/` | `~/.cursor/skills/` |
| Droid | `droid` | `.factory/skills/` | `~/.factory/skills/` |
| Gemini CLI | `gemini-cli` | `.gemini/skills/` | `~/.gemini/skills/` |
| GitHub Copilot | `github-copilot` | `.github/skills/` | `~/.copilot/skills/` |
| Goose | `goose` | `.goose/skills/` | `~/.config/goose/skills/` |
| Kilo Code | `kilo` | `.kilocode/skills/` | `~/.kilocode/skills/` |
| Kiro CLI | `kiro-cli` | `.kiro/skills/` | `~/.kiro/skills/` |
| Neovate | `neovate` | `.neovate/skills/` | `~/.neovate/skills/` |
| OpenCode | `opencode` | `.opencode/skills/` | `~/.config/opencode/skills/` |
| OpenHands | `openhands` | `.openhands/skills/` | `~/.openhands/skills/` |
| Pi | `pi` | `.pi/skills/` | `~/.pi/agent/skills/` |
| Qoder | `qoder` | `.qoder/skills/` | `~/.qoder/skills/` |
| Roo Code | `roo` | `.roo/skills/` | `~/.roo/skills/` |
| Trae | `trae` | `.trae/skills/` | `~/.trae/skills/` |
| Windsurf | `windsurf` | `.windsurf/skills/` | `~/.codeium/windsurf/skills/` |
| Zencoder | `zencoder` | `.zencoder/skills/` | `~/.zencoder/skills/` |

---

## 4. 중복 처리 전략

### 4.1 중복 감지 시점
설치 전 각 에이전트의 대상 경로에서 동일한 이름의 리소스 존재 여부 확인

```typescript
async function checkDuplicate(
  resourceName: string,
  agent: AgentType,
  scope: 'project' | 'global'
): Promise<DuplicateInfo | null>
```

### 4.2 중복 처리 옵션

**인터랙티브 모드** (기본):
```
┌─────────────────────────────────────────────────────────────┐
│ ⚠ 중복 발견: "commit" skill이 이미 존재합니다               │
│                                                             │
│   위치: .claude/skills/commit/SKILL.md                      │
│   생성일: 2024-01-15                                        │
│   소스: vercel-labs/agent-skills                            │
│                                                             │
│ 어떻게 처리할까요?                                          │
│   ○ Skip - 건너뛰기 (기존 유지)                             │
│   ○ Overwrite - 덮어쓰기                                    │
│   ○ Rename - 새 이름으로 저장 (commit-2)                    │
│   ○ Backup - 백업 후 덮어쓰기 (commit.backup)               │
│   ○ Compare - 차이점 보기 후 결정                           │
└─────────────────────────────────────────────────────────────┘
```

**CLI 플래그** (비인터랙티브):
```bash
npx ai-toolkit --skills --on-duplicate=skip      # 건너뛰기
npx ai-toolkit --skills --on-duplicate=overwrite # 덮어쓰기
npx ai-toolkit --skills --on-duplicate=rename    # 자동 넘버링 (skill-2, skill-3)
npx ai-toolkit --skills --on-duplicate=backup    # .backup 생성 후 덮어쓰기
npx ai-toolkit --skills --on-duplicate=fail      # 중복 시 에러 (CI용)
```

### 4.3 Compare 기능 상세

```
┌─────────────────────────────────────────────────────────────┐
│ 📊 "commit" skill 비교                                      │
│                                                             │
│ ┌─ 기존 ──────────────────┬─ 신규 ──────────────────┐       │
│ │ name: commit            │ name: commit            │       │
│ │ description: Git commit │ description: Smart git  │  ← 변경│
│ │ version: 1.0.0          │ version: 1.1.0          │  ← 변경│
│ │                         │ author: vercel          │  ← 추가│
│ └─────────────────────────┴─────────────────────────┘       │
│                                                             │
│ 콘텐츠 변경: 15줄 추가, 3줄 삭제                             │
│                                                             │
│   ○ Skip - 기존 유지                                        │
│   ○ Overwrite - 신규로 교체                                 │
│   ● Merge - 수동 병합 (에디터 열기)                         │
└─────────────────────────────────────────────────────────────┘
```

### 4.4 일괄 처리 (Batch)

여러 리소스에서 중복 발생 시:
```
┌─────────────────────────────────────────────────────────────┐
│ ⚠ 3개 중복 발견                                             │
│                                                             │
│   • commit (Claude Code, Cursor)                            │
│   • review-pr (Claude Code)                                 │
│   • test-runner (Cursor, Cline)                             │
│                                                             │
│ 일괄 처리 방식:                                             │
│   ○ Ask each - 각각 개별 질문                               │
│   ○ Skip all - 모두 건너뛰기                                │
│   ○ Overwrite all - 모두 덮어쓰기                           │
│   ○ Backup all - 모두 백업 후 덮어쓰기                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. 데이터 구조

### 5.1 중복 정보
```typescript
interface DuplicateInfo {
  resourceName: string;
  resourceType: 'skill' | 'rule' | 'command';
  existingPath: string;
  existingMeta: {
    createdAt: Date;
    source?: string;      // 원본 소스 (있으면)
    contentHash: string;  // 내용 해시
  };
  newMeta: {
    source: string;
    contentHash: string;
  };
  isSameContent: boolean; // 해시 비교로 동일 여부
}
```

### 5.2 중복 처리 결과
```typescript
type DuplicateAction = 'skip' | 'overwrite' | 'rename' | 'backup' | 'fail';

interface InstallResult {
  resourceName: string;
  agent: AgentType;
  success: boolean;
  action: 'created' | 'skipped' | 'overwritten' | 'renamed' | 'backed-up';
  path: string;
  backupPath?: string;  // backup 시
  renamedTo?: string;   // rename 시
  error?: string;
}
```

---

## 6. 설치 결과 출력

```
┌─────────────────────────────────────────────────────────────┐
│ ✓ 설치 완료                                                 │
│                                                             │
│ 신규 설치 (4):                                              │
│   ✓ commit → Claude Code, Cursor                            │
│   ✓ review-pr → Claude Code, Cursor                         │
│                                                             │
│ 건너뜀 (1):                                                 │
│   ○ test-runner - 동일 버전 존재                            │
│                                                             │
│ 덮어씀 (1):                                                 │
│   ↻ lint-fix → Cline (백업: lint-fix.backup)                │
│                                                             │
│ 실패 (0)                                                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. 권장 기본값

| 상황 | 기본 동작 |
|------|----------|
| 내용이 동일한 중복 | 자동 Skip (알림만) |
| 내용이 다른 중복 | 인터랙티브 질문 |
| `--yes` 플래그 | Overwrite |
| CI 환경 감지 | Fail (안전) |
